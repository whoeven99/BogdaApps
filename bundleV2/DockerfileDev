FROM node:20-slim
RUN apt-get update -y && apt-get install -y openssl

EXPOSE 3000

WORKDIR /app

COPY package.json package-lock.json* ./

RUN npm install -g npm@latest && \
    npm ci  # 使用 ci 更可靠，严格依据锁文件

ENV NODE_ENV=test

COPY . .

RUN npm run build

CMD ["npm", "run", "docker-start"]
