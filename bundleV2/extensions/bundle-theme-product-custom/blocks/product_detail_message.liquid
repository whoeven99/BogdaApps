{% assign ciwi_source_type = 'app-embed' %}
<div
  class="ciwi-product-message-src"
  data-ciwi-source-type="{{ ciwi_source_type | escape }}"
>
  <script type="application/json" data-ciwi-script="bundles-config">
    {
      "shopifyDomain": "{{ shop.domain }}",
      "moneyFormat": "{{ shop.money_format | escape }}",
      "productId": {{ product.id | default: "null" }},
      "variantIds": [
        {% for item in product.variants %}
          {{ item.id }}{% unless forloop.last %},{% endunless %}
        {% endfor %}
      ],
      "hasSubscription": {% if product.selling_plan_groups and product.selling_plan_groups.size > 0 %}true{% else %}false{% endif %},
      "sellingPlanGroups": [
        {% for group in product.selling_plan_groups %}
          {
            "id": {{ group.id | json }},
            "name": {{ group.name | json }},
            "sellingPlans": [
              {% for selling_plan in group.selling_plans %}
                {
                  "id": {{ selling_plan.id | json }},
                  "name": {{ selling_plan.name | json }}
                }{% unless forloop.last %},{% endunless %}
              {% endfor %}
            ]
          }{% unless forloop.last %},{% endunless %}
        {% endfor %}
      ],
      "marketId": {{ localization.market.id | json }},
      "countryIsoCode": {{ localization.country.iso_code | json }},
      "variants": [
        {% for item in product.variants %}
          {
            "id": "{{ item.id }}",
            "available": {% if item.available %}true{% else %}false{% endif %},
            "compareAtPrice": {{ item.compare_at_price | json }},
            "price": {{ item.price | json }},
            "inventoryQuantity": {{ item.inventory_quantity | default: 0 }},
            "inventoryPolicy": {{ item.inventory_policy | json }},
            "sellingPlanAllocations": [
              {% for allocation in item.selling_plan_allocations %}
                {
                  "price": {{ allocation.price | json }},
                  "compareAtPrice": {{ allocation.compare_at_price | json }},
                  "perDeliveryPrice": {{ allocation.per_delivery_price | json }},
                  "checkoutChargeAmount": {{ allocation.checkout_charge_amount | json }},
                  "remainingBalanceChargeAmount": {{ allocation.remaining_balance_charge_amount | json }},
                  "sellingPlan": {
                    "id": {{ allocation.selling_plan.id | json }},
                    "name": {{ allocation.selling_plan.name | json }}
                  }
                }{% unless forloop.last %},{% endunless %}
              {% endfor %}
            ]
          }{% unless forloop.last %},{% endunless %}
        {% endfor %}
      ],
      "firstVariant": {
        "compareAtPrice": {{ product.variants[0].compare_at_price | json }},
        "price": {{ product.variants[0].price | json }}
      },
      "currencySymbol": "{{ localization.country.currency.symbol | escape }}"
    }
  </script>

  {% assign ciwi_offers_mf = shop.metafields.ciwi_bundle["ciwi-bundle-offers"] %}
  {% if ciwi_offers_mf and ciwi_offers_mf.value != blank %}
    <script type="application/json" data-ciwi-script="bundle-offers">{{ ciwi_offers_mf.value | json }}</script>
  {% endif %}
</div>

<script src="{{ 'product-detail-message.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Ciwi Bundle & Discount",
  "target": "body",
  "enabled_on": {
    "templates": ["product"]
  },
  "stylesheet": "product-detail-message.css"
}
{% endschema %}
